#include <iostream>

using namespace std;

// * n개의 영상을 m개로 나누었을 때, 최대 합이 최소가 되게끔

int main() {
    int lectures[100000];
    int n, m;
    cin >> n >> m;

    for (int i = 0; i < n; ++i) {
        cin >> lectures[i];
    }

    return 0;
}